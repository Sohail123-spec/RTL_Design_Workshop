// TODO: Paste your actual tb_good_mux.v content here.
// The tab was visible in your screenshot but the file was not the active
// tab, so its contents weren't captured. Replace this stub with your real
// testbench before you push — an empty/fake file here would misrepresent
// your work.
//
// Typical shape for this workshop's testbench (VSDIAT iverilog flow):
//   `timescale 1ns / 1ps
//   module tb_good_mux;
//       reg i0, i1, sel;
//       wire y;
//       good_mux uut (.i0(i0), .i1(i1), .sel(sel), .y(y));
//       initial begin
//           $dumpfile("tb_good_mux.vcd");
//           $dumpvars(0, tb_good_mux);
//           // stimulus here
//           #300 $finish;
//       end
//   endmodule
